<!DOCTYPE html>
<html>
<head>
    <style>
        body, img, * {
            background: transparent;
            margin: 0;
            padding: 0;
        }
        
        img {
            float: right;
            margin-right: 22px;
            opacity: 0.5;
            width: 180px;
            height: 90px;
            z-index: 10000;
        }
    </style>
</head>
<body>
    <img src="{{ public_path('/images/cih-logo.svg') }}" alt="{{ public_path('/images/cih-logo.svg') }}">
</body>
</html>
